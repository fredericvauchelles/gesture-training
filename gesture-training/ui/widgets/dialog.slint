import { Palette, VerticalBox } from "std-widgets.slint";
import { CustomPalette } from "styling.slint";

export component CustomDialog inherits Window { 
    callback accept;
    callback cancel;

    public function show() {
        self.visible = true;
    }
}