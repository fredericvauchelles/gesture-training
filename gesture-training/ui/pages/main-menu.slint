import { Palette, StyleMetrics, VerticalBox, Button, HorizontalBox, StandardListView } from "std-widgets.slint";
import { RadioBox, Label, Icons } from "../widgets/widgets.slint";

export component MainMenu inherits VerticalBox {
    callback show-add-folder <=> button_add_folder.clicked;

    padding-left: 5% * root.width;
    padding-right: 5% * root.width;
    padding-top: 5% * root.height;
    padding-bottom: 5% * root.height;

    VerticalBox {
        padding-bottom: 20px;

        list := StandardListView {

        }
    }
    
    VerticalBox {
        Label {
            text: "Image Sources";
        }
        HorizontalBox {
            button_add_folder := Button {
                //shape: ThemeButtonShape.None;
                icon: Icons.folder;
                colorize-icon: true;
                width: 50px;
                height: 50px;
            }
            button_add_zip := Button {
                //shape: ThemeButtonShape.Right;
                icon: Icons.zip-file;
                colorize-icon: true;
                width: 50px;
                height: 50px;
            }
        }
        Label {
            text: "Images";
        }
        RadioBox {
            min-height: 50px;

            selected-value: 5;
            datas: [
                {value: 5, display: "5"},
                {value: 10, display: "10"},
                {value: 15, display: "15"},
                {value: 20, display: "20"},
                {value: 25, display: "25"},
            ];
        }
        Label {
            text: "Durations";
        }
        RadioBox {
            min-height: 50px;

            selected-value: 30;
            datas: [
                {value: 30, display: "30''"},
                {value: 60, display: "1'"},
                {value: 120, display: "2'"},
                {value: 180, display: "3'"}, 
                {value: 300, display: "5'"}, 
                {value: 600, display: "10'"}, 
                {value: 600, display: "20'"}, 
            ];
        }
    }
    
    HorizontalBox {
        Button {
            text: "Start";
        }
    }
}