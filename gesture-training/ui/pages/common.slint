import { StatusIconData } from "../widgets/widgets.slint";

export enum ImageSourceType {
    Folder,
    Archive,
}

export struct PreparedSessionData {
    status: StatusIconData,
    image-count: int,
    /// seconds
    image-duration: int,
}